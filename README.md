# Название проекта

Банковское приложение

## Пароли 

Пароли от всех тестовых пользователей - password123 

### Примечание

1) При трансфере сейчас не работает проверка ключа иденпотентности, так как я хранение
логов перенёс в Elastic, а получение данных с Elastica, в том числе проверка ключа
идемпотентности в Elastic пока ещё не реализована!! Работало только, когда логи в Бд Postgres хранились
2) Интеграция Elastic + Logstash + Kafka работает но надо тестировать и настраивать её для
высоконагруженных сценариев с низкой летентностью запросов

#### Установка

Как установить и запустить проект.

```cmd

Создаём образ приложения

docker build -t spring-app .

Запускаем docker-compose

docker-compose up -d